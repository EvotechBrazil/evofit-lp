# Webhook n8n — Lead formulário EvoFit (server-side only)
LEAD_WEBHOOK_URL=https://n8n.evotechsystem.cloud/webhook/evofit-lead-v1
LEAD_WEBHOOK_SECRET=gere_um_segredo_longo_com_openssl_rand_hex_32

# Opcional: origens permitidas no POST /api/lead (CSV)
# LEAD_ALLOWED_ORIGINS=https://evofit.tech,https://www.evofit.tech,https://site.evofit.tech,http://localhost:3000

# Opcional: alerta Slack/Discord quando o n8n falhar
# LEAD_ALERT_WEBHOOK=https://hooks.slack.com/services/...

# Catálogo vivo — Alicia (GET /api/v1/public/product)
# Sem isto o site cai no fallback estático de content/site.ts
ALICIA_PUBLIC_API_URL=http://localhost:3001/api/v1

# HMAC do ping de revalidate (mesmo valor em SITE_REVALIDATE_SECRET da Alicia)
SITE_REVALIDATE_SECRET=gere_um_segredo_longo_com_openssl_rand_hex_32
